syntax = "proto3";

package com.linbit.linstor.proto.javainternal;

// LinStor - Internal message containing a (maybe encrypted) shared secret
message MsgIntAuthSecret
{
    // Shared secret
    bytes   shared_secret = 1;
}
