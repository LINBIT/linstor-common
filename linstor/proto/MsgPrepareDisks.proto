syntax = "proto2";

package com.linbit.linstor.proto;

// linstor - triggers detect and preparing NVME and PMEM
message MsgPrepareDisks
{
    optional string nvme_filter = 1 [default = "^INTEL SSDPE21.*"];
    optional bool detect_pmem = 2 [default = true];
}
