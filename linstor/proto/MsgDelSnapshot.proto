syntax = "proto2";

package com.linbit.linstor.proto;

// linstor - Delete snapshot
message MsgDelSnapshot
{
    // UUID
    optional string uuid = 1;

    // Resource name
    required string rsc_name = 2;

    // Snapshot name
    required string snapshot_name = 3;
}
