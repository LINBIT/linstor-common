syntax = "proto2";

package com.linbit.linstor.proto.responses;
import "linstor/proto/common/ApiCallResponse.proto";

// Single generic ApiCallResponse
// Requested by: too many to list them here, also the list will most likely be extended regularly
message MsgGenericResponse
{
    required common.ApiCallResponse response = 1;
}
