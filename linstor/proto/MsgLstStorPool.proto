syntax = "proto2";

package com.linbit.linstor.proto;
import public "linstor/proto/StorPool.proto";

// linstor - List storage pools
message MsgLstStorPool
{
    // Storage pools
    repeated StorPool stor_pools = 1;

	// Storage pools namespaces to list
	repeated string namespaces = 2;
}
