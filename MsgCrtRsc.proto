syntax = "proto2";

package com.linbit.linstor.proto;
import public "LinStorMapEntry.proto";
import public "Vlm.proto";


// linstor - Create resource
message MsgCrtRsc
{
    // UUID
    optional bytes rsc_uuid = 1;

    // Node uuid
    optional bytes node_uuid = 2;

    // Node name
    required string node_name = 3;

    // Resource definition uuid
    optional bytes rsc_dfn_uuid = 4;

    // Resource name
    required string rsc_name = 5;

    // Resource properties map
    repeated LinStorMapEntry rsc_props = 6;

    // Volume
    repeated Vlm vlms = 7;
}
